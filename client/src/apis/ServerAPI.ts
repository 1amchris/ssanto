import ServerCom from './ServerCom';

// TODO: uncomment this and connect with application. It crashes at the moment
// const server = new ServerCom();
// setTimeout(() => {
//   server.open();

//   server.subscribe('myVar', (data: any) => {
//     console.log('`myVar` value is ' + data);
//   });

//   server.callFunction('function');
//   server.callMethod('a_class', 'method');
// }, 1000);
